[project]
name = "guarded-agent-chat-backend"
version = "0.1.0"
description = "Chat agent behind an LLM SQL guardrail"
requires-python = ">=3.12"
dependencies = [
    "crewai[litellm]>=1.15.14",
    "fastapi>=0.115",
    "python-dotenv>=1.0",
    "sqlalchemy>=2.0",
    "uvicorn>=0.30",
]
